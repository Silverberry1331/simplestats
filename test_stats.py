from stats import mean

def test_mean():
    """Test some standard behavior of the mean() function."""
    assert(mean([2, 4]) == 3)

def test_float_mean():
    """Test some standard behavior when the result is not an integer."""
    assert(mean([1, 2]) == 1.5)
	
test_mean()
test_float_mean()

def test_mean1()
    """test mean in separate test"""
    assert (mean([0,0]) == 0 )
	
	
	HELLO How are you?  I am fine.